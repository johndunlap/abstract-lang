package abs.compiler.parser.oop;

import abs.compiler.parser.Node;
import abs.compiler.parser.RootNode;

public class OopRootNode extends RootNode {
    public OopRootNode(Node node) {
        super(node);
    }
}
